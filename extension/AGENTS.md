# Component ownership

Read `../docs/genie_context.md`, `../docs/integration_contract.md`, `../docs/extension_ui.md`, and `../docs/extension_setup.md` before changes.

Work within `extension/` and its owned setup document. Keep dependency and lockfile changes inside this component. Coordinate API changes with the other owner. Never commit secrets. Run this component's typecheck, build, and relevant tests before handoff.
