# Detail-page HTML samples

These are user-supplied FCI detail-page HTML snapshots for S4P Synergy and Released. SVG artwork and the Reviews section were removed to keep only the relevant page structure and listing information. These snapshots are parser inputs, not evidence of current service activity.

The Released resource URL has not been supplied. Tests use an explicitly synthetic route ID. Production extraction reads the actual browser route.
